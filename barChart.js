var options = {
  chartTitle: 'My Chart',
  chartWidth: 500,
  chartHeight: 400,
  barSpacing: 3,
  barColor: 'orange',
  barLabelColor: 'blue',
  barValuePos: 'bottom'
};

function drawBarChart(data, options, element){
  console.log(options);
  console.log(data[3][1]);
}
